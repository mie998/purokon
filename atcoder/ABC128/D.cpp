#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    int i, j, l;
    cin >> n >> k;
    deque<int> dequeue;
    for (i = 0; i < n; i++) {
        cin >> j;
        dequeue.push_back(j);
    }
}
